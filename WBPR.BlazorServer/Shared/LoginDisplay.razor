@using WBPR.Base.Config
@inject NavigationManager NavigationManager

<CascadingAuthenticationState>
    <AuthorizeView>
        <Authorized>
            <MudText>Hello, <b></b></MudText>
            <MudMenu Icon="@Icons.Filled.AccountCircle">
                <MudMenuItem Icon="@Icons.Filled.Logout" Color="Color.Inherit" Edge="Edge.Start" OnClick="Logout">Logout</MudMenuItem>
            </MudMenu>
        </Authorized>
        <NotAuthorized>
            <MudIconButton Icon="@Icons.Filled.Login" Color="Color.Inherit" Edge="Edge.Start" OnClick="Login" />
        </NotAuthorized>
    </AuthorizeView>
</CascadingAuthenticationState>

@code{

    private void Logout()
    {
        NavigationManager.NavigateTo(Constant.MS_LOGOUT_URL, forceLoad: true);
    }

    private void Login()
    {
        NavigationManager.NavigateTo(Constant.MS_LOGIN_URL, forceLoad: true);
    }
}