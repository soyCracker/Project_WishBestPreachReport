namespace WBPR.Service.Services
{
    public class GraphClientUtilService
    {
        //private readonly TokenProvider tokenProvider;
        public GraphClientUtilService()
        {
            //this.tokenProvider = tokenProvider;
        }

        //public async Task<string> GetRoot()
        //{
        //    string accessToken = tokenProvider.AccessToken;

        //    // Use the access token to call a protected web API.
        //    HttpClient client = new HttpClient();
        //    client.DefaultRequestHeaders.Authorization = new AuthenticationHeaderValue("Bearer", accessToken);
        //    string url = "https://graph.microsoft.com/v1.0/me/drive/root/children";
        //    string json = await client.GetStringAsync(url);
        //    return json;
        //}
    }
}
